ADMIN_EMAIL="admin@fdmediaglobal.cloud"
ADMIN_PASSWORD="change-me"
AUTH_SECRET="change-me-long-random"
MONITOR_INTERVAL_SECONDS="60"

# Opcional — alertas por correo (Agente). Mismo bloque en app y worker vía docker-compose.
# SMTP_HOST="smtp.ejemplo.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_FROM="alertas@fdmediaglobal.cloud"
# SMTP_USER=""
# SMTP_PASS=""

